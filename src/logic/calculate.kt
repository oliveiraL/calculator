package logic

fun calculate(next: String, total: String, operation: String, buttonName: String, update: (String, String, String) -> Unit): Unit = run {
    if (isNumber(buttonName)) {

    } else {
    }
}

